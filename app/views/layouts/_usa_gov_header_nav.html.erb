<nav aria-label="Topic" class="">
  <div class="flowIndicator">
    <div class="<%= "active" if current_step == 1 %> right "><i class="fa fa-check-circle"></i><span>ELIGIBILITY</span></div>
    <div class="<%= "active" if current_step == 2 %> left right <%= "notInitiated" if current_step < 2 %>"><i class="fa fa-user"></i><span>IDENTITY</span></div>
    <div class="<%= "active" if current_step == 3 %> left right <%= "notInitiated" if current_step < 3 %>"><i class="fa fa-envelope "></i><span>MORE INFO</span></div>
    <div class="<%= "active" if current_step == 5 %> left <%= "notInitiated" if current_step < 5 %>"><i class="fa fa-thumbs-o-up"></i><span>COMPLETE</span></div>  
  </div>  
</nav>